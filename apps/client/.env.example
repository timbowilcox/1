# App Mode: 'sandbox' | 'production'
NEXT_PUBLIC_APP_MODE=

# Base URL of the API server (no trailing slash). In production this is your
# API origin, e.g. https://api.yourdomain.com
NEXT_PUBLIC_API_URL=http://localhost:4000
